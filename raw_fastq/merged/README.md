# Concatenated raw fastq data 

Interleaved fastq data, raw, concatenated across libraries. 

Files:

- `IDS.list`: list of biological replicates
- `*.fastq`: concatenated fastq

Directories: 

- `profiles`: Metaphlam.sh output profiles
